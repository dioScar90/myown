@model IEnumerable<myown.Models.Serpent>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PopularName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CientificName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FamilyType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @* @{ string popularName = item.PopularName.UcFirst(); }
                @Html.DisplayFor(modelItem => popularName) *@
                @Html.DisplayFor(modelItem => item.PopularName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CientificName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FamilyType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>